alias bat="batcat"
alias fd="fdfind"

set -x PATH $PATH ~/.local/bin
set -x PATH $PATH ~/site/venv/bin
set -x VIRTUAL_ENV "{{ site_path }}/venv"
set -x DJANGO_SETTINGS_MODULE "{{ django_settings_module }}"
cd site
